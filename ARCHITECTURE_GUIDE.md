# 🏗️ Architecture Complète & Design Patterns

## Table des matières
1. [Architecture générale](#architecture-générale)
2. [Flux de requête](#flux-de-requête)
3. [Couches applicatives](#couches-applicatives)
4. [Design patterns utilisés](#design-patterns-utilisés)
5. [Flows visuels](#flows-visuels)

---

## Architecture générale

### Diagramme d'architecture

```
┌─────────────────────────────────────────────────────────────────┐
│                        CLIENT (Frontend/Mobile)                  │
└────────────────────────────┬──────────────────────────────────────┘
                             │ HTTP/HTTPS
                             ▼
┌─────────────────────────────────────────────────────────────────┐
│                    EXPRESS.JS SERVER (Port 8080)                │
│  ┌────────────────────────────────────────────────────────────┐ │
│  │         ROUTING LAYER (router/*.js)                       │ │
│  │  ┌──────────────────────────────────────────────────────┐ │ │
│  │  │  GET /users/list    PUT /products/update/{id}        │ │ │
│  │  │  POST /users/login  DELETE /orders/{id}              │ │ │
│  │  └──────────────────────────────────────────────────────┘ │ │
│  └────────────────────────────┬───────────────────────────────┘ │
│                               ▼                                  │
│  ┌────────────────────────────────────────────────────────────┐ │
│  │    MIDDLEWARE LAYER                                       │ │
│  │  ┌──────────────────┐  ┌──────────────────────────────┐  │ │
│  │  │  Authentication  │  │ Authorization (Role Check)   │  │ │
│  │  │  (JWT Token)     │  │ (admin, superadmin)          │  │ │
│  │  └──────────────────┘  └──────────────────────────────┘  │ │
│  │  ┌──────────────────┐  ┌──────────────────────────────┐  │ │
│  │  │  Input Validation│  │  Error Handling              │  │ │
│  │  │  (express-val)   │  │  (try-catch)                 │  │ │
│  │  └──────────────────┘  └──────────────────────────────┘  │ │
│  └────────────────────────────┬───────────────────────────────┘ │
│                               ▼                                  │
│  ┌────────────────────────────────────────────────────────────┐ │
│  │    CONTROLLER LAYER (controller/*.js)                    │ │
│  │  ┌──────────────────────────────────────────────────────┐ │ │
│  │  │  - getUserById()      - createProduct()              │ │ │
│  │  │  - updateUser()       - deleteOrder()                │ │ │
│  │  │  - Business Logic     - Data transformation          │ │ │
│  │  └──────────────────────────────────────────────────────┘ │ │
│  └────────────────────────────┬───────────────────────────────┘ │
│                               ▼                                  │
│  ┌────────────────────────────────────────────────────────────┐ │
│  │    MODEL/ORM LAYER (models/*.js)                         │ │
│  │  ┌──────────────────────────────────────────────────────┐ │ │
│  │  │  Sequelize ORM                                       │ │ │
│  │  │  - User.findByPk(id)   - Product.create(data)        │ │ │
│  │  │  - Order.update()      - Validation & Hooks          │ │ │
│  │  └──────────────────────────────────────────────────────┘ │ │
│  └────────────────────────────┬───────────────────────────────┘ │
│                               ▼                                  │
│  ┌────────────────────────────────────────────────────────────┐ │
│  │              HELPER LAYER (helpers/*.js)                  │ │
│  │  ┌──────────────────┐  ┌──────────────────────────────┐  │ │
│  │  │  sendEmail()     │  │  successResponse()           │  │ │
│  │  │  (Nodemailer)    │  │  errorResponse()             │  │ │
│  │  └──────────────────┘  └──────────────────────────────┘  │ │
│  └────────────────────────────────────────────────────────────┘ │
│                               ▼                                  │
│  ┌────────────────────────────────────────────────────────────┐ │
│  │              RESPONSE FORMATTER                           │ │
│  │  {                                                       │ │
│  │    statusCode: 200,                                    │ │
│  │    message: "Success",                                 │ │
│  │    data: {...}                                         │ │
│  │  }                                                      │ │
│  └────────────────────────────────────────────────────────────┘ │
└─────────────────────────────┬──────────────────────────────────────┘
                             │ JSON Response
                             ▼
┌─────────────────────────────────────────────────────────────────┐
│                      CLIENT RECEIVES RESPONSE                    │
└─────────────────────────────────────────────────────────────────┘
                             │
                             ▼ (Database interaction below)
┌─────────────────────────────────────────────────────────────────┐
│                    MYSQL DATABASE                               │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐          │
│  │ users table  │  │ products tbl │  │ orders table │          │
│  │ ┌──────────┐ │  │ ┌──────────┐ │  │ ┌──────────┐ │          │
│  │ │id (PK)   │ │  │ │id (PK)   │ │  │ │id (PK)   │ │          │
│  │ │name      │ │  │ │name      │ │  │ │user_id   │ │          │
│  │ │email     │ │  │ │price     │ │  │ │product_id│ │          │
│  │ │password  │ │  │ │category  │ │  │ │quantity  │ │          │
│  │ └──────────┘ │  │ └──────────┘ │  │ └──────────┘ │          │
│  └──────────────┘  └──────────────┘  └──────────────┘          │
└─────────────────────────────────────────────────────────────────┘
```

---

## Flux de requête

### Étape par étape

```
1. CLIENT
   ├─ Envoie une requête HTTP
   │  ├─ GET /users/123
   │  ├─ POST /products (+ body)
   │  └─ Authorization: Bearer <token>
   │
2. SERVER RECEIVE
   ├─ Parse le HTTP request
   ├─ Route la requête
   │
3. MIDDLEWARE CHAIN
   ├─ Authentication (vérifier le token JWT)
   ├─ Authorization (vérifier les rôles)
   ├─ Validation (vérifier les données)
   │
4. CONTROLLER
   ├─ Exécute la logique métier
   ├─ Appelle les models
   │
5. MODEL/ORM
   ├─ Prépare la requête SQL
   ├─ Interroge la BD
   │
6. DATABASE
   ├─ Exécute la requête
   ├─ Retourne les résultats
   │
7. RESPONSE
   ├─ Formate la réponse
   ├─ Ajoute headers
   ├─ Envoie JSON
   │
8. CLIENT RECEIVE
   ├─ Parse la réponse
   ├─ Affiche les données
```

### Code complet d'une requête

```javascript
// ========== 1. CLIENT REQUEST ==========
// GET http://localhost:8080/users/123
// Headers: Authorization: Bearer eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...

// ========== 2. ROUTING ==========
// routes/userRouter.js
userRouter.get('/details/:user_id', getUserByuser_id);

// ========== 3. MIDDLEWARE ==========
// Automatically handled by Express

// ========== 4. CONTROLLER ==========
// controller/userController.js
module.exports.getUserByuser_id = async (req, res) => {
    try {
        const { user_id } = req.params;                    // Extract from URL
        const user = await User.findByPk(user_id);         // Query database
        
        if (!user) {
            return res.status(404).json(
                errorResponse(404, 'User not found')
            );
        }
        
        res.status(200).json(
            successResponse(200, 'User retrieved', user)    // Format response
        );
    } catch (error) {
        res.status(500).json(
            errorResponse(500, 'Server error')
        );
    }
};

// ========== 5. MODEL ==========
// models/user.js
const user = await User.findByPk(user_id);
// Translates to SQL:
// SELECT * FROM users WHERE user_id = 123;

// ========== 6. RESPONSE ==========
// {
//     "statusCode": 200,
//     "message": "User retrieved",
//     "data": {
//         "user_id": 123,
//         "user_name": "John Doe",
//         "user_email": "john@example.com",
//         ...
//     }
// }
```

---

## Couches applicatives

### 1. Route Layer (Routing)

```javascript
// routes/userRouter.js
const userRouter = express.Router();
userRouter.get('/list', getAll);
userRouter.post('/create', authenticate, create);
userRouter.put('/:id', authenticate, update);
userRouter.delete('/:id', authenticate, delete);
```

**Responsabilités:**
- Mapper les URLs aux controllers
- Appliquer les middlewares
- Grouper les routes

---

### 2. Middleware Layer

```javascript
// middleware/authentication.js
const jwt = require('jsonwebtoken');

function authenticate(req, res, next) {
    const token = req.header('Authorization')?.split(' ')[1];
    
    if (!token) {
        return res.status(401).json(errorResponse(401, 'No token'));
    }
    
    try {
        const decoded = jwt.verify(token, process.env.ACCESS_TOKEN_SECRET);
        req.user = decoded;
        next();
    } catch (error) {
        res.status(401).json(errorResponse(401, 'Invalid token'));
    }
}
```

**Responsabilités:**
- Authentification (JWT)
- Autorisation (rôles)
- Validation des données
- Gestion des erreurs

---

### 3. Controller Layer

```javascript
// controller/userController.js
module.exports.getUser = async (req, res) => {
    try {
        // 1. Extraire les paramètres
        const { user_id } = req.params;
        
        // 2. Valider les données (optionnel)
        if (!user_id) {
            return res.status(400).json(errorResponse(400, 'Invalid ID'));
        }
        
        // 3. Appeler le model
        const user = await User.findByPk(user_id);
        
        // 4. Vérifier le résultat
        if (!user) {
            return res.status(404).json(errorResponse(404, 'Not found'));
        }
        
        // 5. Retourner la réponse
        res.status(200).json(successResponse(200, 'Success', user));
    } catch (error) {
        res.status(500).json(errorResponse(500, 'Server error'));
    }
};
```

**Responsabilités:**
- Extraire les données de la requête
- Appeler les models
- Exécuter la logique métier
- Formater la réponse

---

### 4. Model/ORM Layer

```javascript
// models/user.js
module.exports = (sequelize, DataTypes) => {
    const User = sequelize.define('User', {
        user_id: {
            type: DataTypes.INTEGER,
            primaryKey: true,
            autoIncrement: true
        },
        user_name: DataTypes.STRING,
        user_email: DataTypes.STRING,
        user_password: DataTypes.STRING
    });
    
    return User;
};

// Utilisation :
User.findByPk(1);              // SELECT WHERE id = 1
User.findAll();                // SELECT *
User.create({...});            // INSERT
user.update({...});            // UPDATE
user.destroy();                // DELETE
```

**Responsabilités:**
- Définir la structure des données
- Valider les données
- Exécuter les requêtes BD
- Gérer les relations

---

### 5. Helper Layer

```javascript
// helpers/successAndError.js
module.exports.successResponse = (statusCode, message, data) => {
    return {
        statusCode,
        message,
        data
    };
};

module.exports.errorResponse = (statusCode, message) => {
    return {
        statusCode,
        message,
        data: null
    };
};

// helpers/sendEmail.js
module.exports.sendEmail = async (data) => {
    // Envoyer un email via Nodemailer
};
```

**Responsabilités:**
- Fonctions utilitaires réutilisables
- Formatage de réponses
- Envoi d'emails
- Opérations communes

---

## Design patterns utilisés

### 1. MVC Pattern

```
Model      → Données & Logique BD
View       → (Non utilisé en API)
Controller → Logique métier & Requête/Réponse
```

### 2. Repository Pattern (Implicite dans Sequelize)

```javascript
// User = Repository
User.findByPk()      // Récupérer 1 user
User.findAll()       // Récupérer tous
User.create()        // Créer
User.update()        // Mettre à jour
```

### 3. Middleware Pattern

```javascript
app.use(helmet());              // Sécurité
app.use(express.json());        // Parse JSON
app.use(cors());                // CORS
app.use(authenticate);          // Auth
app.use('/users', userRouter);  // Routing
```

### 4. Error Handling Pattern

```javascript
try {
    // Logique
} catch (error) {
    // Gestion centralisée
    res.status(500).json(errorResponse(500, error.message));
}
```

---

## Flows visuels

### Flow: Création d'un user

```
┌─────────────────────────────────────────────────────────────┐
│  CLIENT                                                     │
│  POST /users/register                                       │
│  {                                                          │
│    "user_name": "John",                                     │
│    "user_email": "john@email.com",                          │
│    "user_password": "pass123"                               │
│  }                                                          │
└────────────────────────┬────────────────────────────────────┘
                         │
                         ▼
┌─────────────────────────────────────────────────────────────┐
│  ROUTE HANDLER                                              │
│  routes/userRouter.js                                       │
│  POST → insertNewUser controller                            │
└────────────────────────┬────────────────────────────────────┘
                         │
                         ▼
┌─────────────────────────────────────────────────────────────┐
│  CONTROLLER                                                 │
│  1. Extract body data                                       │
│  2. Hash password: bcryptjs.hash()                          │
│  3. Call model: User.create()                               │
└────────────────────────┬────────────────────────────────────┘
                         │
                         ▼
┌─────────────────────────────────────────────────────────────┐
│  MODEL / SEQUELIZE                                          │
│  1. Validate data                                           │
│  2. Generate SQL INSERT                                     │
│  3. Execute on database                                     │
└────────────────────────┬────────────────────────────────────┘
                         │
                         ▼
┌─────────────────────────────────────────────────────────────┐
│  DATABASE                                                   │
│  INSERT INTO users (user_name, user_email, user_password)  │
│  VALUES ('John', 'john@email.com', '$2a$15$...')            │
│                                                             │
│  RETURN: id = 1                                             │
└────────────────────────┬────────────────────────────────────┘
                         │
                         ▼
┌─────────────────────────────────────────────────────────────┐
│  RESPONSE                                                   │
│  {                                                          │
│    "statusCode": 201,                                       │
│    "message": "User registered successfully",               │
│    "data": {                                                │
│      "user_id": 1,                                          │
│      "user_name": "John",                                   │
│      "user_email": "john@email.com"                         │
│    }                                                        │
│  }                                                          │
└────────────────────────┬────────────────────────────────────┘
                         │
                         ▼
┌─────────────────────────────────────────────────────────────┐
│  CLIENT RECEIVES RESPONSE                                   │
│  (Frontend processes data and stores user info)             │
└─────────────────────────────────────────────────────────────┘
```

### Flow: Login & JWT Tokens

```
┌─────────────────────────────────────────────────┐
│  CLIENT LOGIN                                   │
│  POST /users/login                              │
│  { "user_email": "john@email.com",              │
│    "user_password": "pass123" }                 │
└────────────────┬────────────────────────────────┘
                 │
                 ▼
┌─────────────────────────────────────────────────┐
│  FIND USER by email                             │
│  User.findOne({ where: { user_email } })        │
└────────────────┬────────────────────────────────┘
                 │
                 ▼
┌─────────────────────────────────────────────────┐
│  COMPARE PASSWORD                               │
│  bcryptjs.compare(password, hashedPassword)     │
│  Match? ✓ YES → Continue                        │
└────────────────┬────────────────────────────────┘
                 │
                 ▼
┌─────────────────────────────────────────────────┐
│  GENERATE TOKENS                                │
│  accessToken = jwt.sign(                        │
│    { user_id, user_email },                     │
│    SECRET,                                      │
│    { expiresIn: '1d' }                          │
│  )                                              │
│  refreshToken = jwt.sign(...)                   │
└────────────────┬────────────────────────────────┘
                 │
                 ▼
┌─────────────────────────────────────────────────┐
│  RETURN TOKENS                                  │
│  {                                              │
│    "statusCode": 200,                           │
│    "data": {                                    │
│      "user": {...},                             │
│      "accessToken": "eyJhbGc...",               │
│      "refreshToken": "eyJhbGc..."               │
│    }                                            │
│  }                                              │
└────────────────┬────────────────────────────────┘
                 │
                 ▼
┌─────────────────────────────────────────────────┐
│  CLIENT STORES TOKENS                           │
│  localStorage.setItem('token', accessToken)     │
└─────────────────────────────────────────────────┘
```

### Flow: Requête authentifiée

```
┌─────────────────────────────────────────────────┐
│  CLIENT REQUEST                                 │
│  GET /users/details/1                           │
│  Headers: Authorization: Bearer <accessToken>   │
└────────────────┬────────────────────────────────┘
                 │
                 ▼
┌─────────────────────────────────────────────────┐
│  MIDDLEWARE: AUTHENTICATION                     │
│  1. Extract token from header                   │
│  2. Verify token with jwt.verify()              │
│  3. Valid? ✓ → attach user to req              │
│     Invalid? ✗ → 401 Unauthorized               │
└────────────────┬────────────────────────────────┘
                 │
                 ▼
┌─────────────────────────────────────────────────┐
│  CONTROLLER                                     │
│  getUserByuser_id()                             │
│  const { user_id } = req.params                 │
│  const user = User.findByPk(user_id)            │
└────────────────┬────────────────────────────────┘
                 │
                 ▼
┌─────────────────────────────────────────────────┐
│  RESPONSE                                       │
│  {                                              │
│    "statusCode": 200,                           │
│    "message": "User retrieved",                 │
│    "data": { user data }                        │
│  }                                              │
└────────────────┬────────────────────────────────┘
                 │
                 ▼
┌─────────────────────────────────────────────────┐
│  CLIENT USES TOKEN IN HEADER                    │
│  All authenticated requests use:                │
│  Authorization: Bearer <accessToken>            │
└─────────────────────────────────────────────────┘
```

---

## Summary

- **Route** → URLs et Middlewares
- **Middleware** → Authentification & Validation
- **Controller** → Logique métier
- **Model** → Données & BD
- **Helper** → Fonctions utilitaires
- **Response** → Format standardisé

Ce design permet une **séparation des responsabilités** et rend le code **maintenable et scalable**! 🚀
